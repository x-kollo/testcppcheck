# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "main" branch
  push:
    branches: [ "main" ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write
  
# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      - name: Git clone project
        uses: actions/checkout@v2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: cppcheck
        run: |
          sudo apt install cppcheck
          cppcheck --enable=all --xml --output-file=cppcheck_report.xml .
          cppcheck-htmlreport --file cppcheck_report.xml --report-dir=cppcheck_report
          ls
          cd cppcheck_report
          ls
      - name: Setup Pages
        uses: actions/configure-pages@v4
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: 'cppcheck_report/.'
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
  
      # - name: HTML Preview
      #   id: html_preview
      #   uses: pavi2410/html-preview-action@v2
      #   with:
      #     html_file: 'cppcheck_report/index.html'
      # - name: Configure Git
      #   run: |
      #     git config --global user.name "x-k0llo"
      #     git config --global user.email "novak.bogdanius@gmail.com"
      # - name: Commit and Push to gh-pages
      #   run: |
      #     git checkout -b gh-pages
      #     git add cppcheck_report/.
      #     git commit -m "Publish HTML file from GitHub Actions"
      #     git push origin gh-pages
      # # - name: Get HTML preview link
      #   uses: github-actions-up-and-running/pr-comment@v1.0.0
      #   with:
      #     repo-token: ${{ secrets.GITHUB_TOKEN }}
      #     message: ${{ format('[Click here to preview HTML page in browser]({0})', steps.html_preview.outputs.url) }}